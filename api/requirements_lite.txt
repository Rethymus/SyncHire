# SyncHire Lite - Minimal Dependencies
# Local-first job application tool without cloud infrastructure

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.0
pydantic-settings==2.6.0

# Database (SQLite only - lightweight)
sqlalchemy==2.0.36
aiosqlite==0.20.0

# HTTP Client (for AI APIs)
httpx==0.27.2
aiohttp==3.11.10

# File Processing
python-multipart==0.0.20
PyPDF2==3.0.1
python-docx==1.1.2

# AI APIs (Network-based - preserved)
openai==1.57.0
anthropic==0.42.0

# Utilities
python-dotenv==1.0.1
pytz==2024.2

# Logging
python-json-logger==2.0.7

# Security (Minimal - local tool)
cryptography==44.0.0

# Data Processing
numpy==2.1.3
pandas==2.2.3

# MCP Client (For local MCP servers)
mcp==0.9.0

# Removed Dependencies (No longer needed):
# - asyncpg (PostgreSQL - replaced with aiosqlite)
# - redis (Redis - removed, using in-memory)
# - aiobotocore (Minio/S3 - removed, using local filesystem)
# - python-jose (JWT - removed, no authentication)
# - passlib (Password hashing - removed, no authentication)
# - pyotp (2FA - removed, no authentication)
# - aiosmtplib (Email - removed, no email service)
# - alembic (Database migrations - simplified for local)
# - pgvector (Vector DB - using local alternatives)
